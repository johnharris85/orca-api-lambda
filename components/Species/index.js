// Step 2: Create Species List
// -----------------------
// Using axios send a GET request to the address: https://hotline.whalemuseum.org/api.json
// Once the data is returned console.log it and review the structure.
// The data is an array of sightings, across many different species.
// Create an array of the different unique species and then create a species
// component (like the one below) for each one and add it to the DOM
// under the .species-list element.
//
//  The tab component should look like this:
//    <div class="species">{SPECIES NAME}</div>

